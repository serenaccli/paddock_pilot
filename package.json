{
  "name": "paddock-pilot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/patch-piper.mjs",
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --mode github-pages && cp dist/source/index.html dist/index.html",
    "publish:branch-pages": "npm run build:pages && cp dist/index.html index.html && cp -R dist/assets . && cp -R dist/piper . && cp -R dist/models . && cp dist/sw.js sw.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mintplex-labs/piper-tts-web": "^1.0.4",
    "@tensorflow-models/coco-ssd": "^2.2.3",
    "@tensorflow/tfjs": "^4.22.0",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0"
  }
}
